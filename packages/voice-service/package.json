{
  "name": "@iris/voice-service",
  "version": "0.1.0",
  "private": true,
  "deprecated": "Use voice-backend WebSocket directly (ws://localhost:8001/ws/voice). This package adds unnecessary latency.",
  "description": "[DEPRECATED] WebSocket bridge for IRIS voice - use voice-backend directly",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ws": "^8.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
